namespace PipelinesExplorer.VisualStudio;

/// <summary>
/// Brand constants. <see cref="ProductName"/> is intentionally NOT localized:
/// "Pipelines Explorer" is the product name and stays the same in every UI
/// language.
/// </summary>
internal static class Branding
{
    public const string ProductName = "Pipelines Explorer";
}
