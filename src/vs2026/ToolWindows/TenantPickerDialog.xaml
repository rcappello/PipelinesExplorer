<DataTemplate xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
              xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
              xmlns:platform="clr-namespace:Microsoft.VisualStudio.PlatformUI;assembly=Microsoft.VisualStudio.Shell.15.0">
    <DataTemplate.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource {x:Static platform:EnvironmentColors.ToolWindowTextBrushKey}}" />
            <Setter Property="FontFamily" Value="{DynamicResource VsFont.EnvironmentFontFamily}" />
            <Setter Property="FontSize"   Value="{DynamicResource VsFont.EnvironmentFontSize}" />
            <Setter Property="TextOptions.TextFormattingMode" Value="Display" />
        </Style>

        <!-- Item template for the ComboBox: stacks the title (display name) and a
             muted subtitle (default domain + tenant id) for clarity. -->
        <DataTemplate x:Key="TenantItemTemplate">
            <StackPanel Orientation="Vertical" Margin="2,3,2,3">
                <TextBlock Text="{Binding Title}" FontWeight="SemiBold" />
                <TextBlock Text="{Binding Subtitle}"
                           Opacity="0.75"
                           FontSize="11" />
            </StackPanel>
        </DataTemplate>
    </DataTemplate.Resources>

    <StackPanel Orientation="Vertical"
                MinWidth="420"
                Margin="12,12,12,8">
        <TextBlock Text="Select the Microsoft Entra tenant to use for Azure DevOps."
                   TextWrapping="Wrap"
                   Margin="0,0,0,10" />

        <ComboBox ItemsSource="{Binding Choices}"
                  SelectedItem="{Binding SelectedChoice, Mode=TwoWay}"
                  ItemTemplate="{StaticResource TenantItemTemplate}"
                  MaxDropDownHeight="380"
                  HorizontalAlignment="Stretch"
                  MinHeight="32" />
    </StackPanel>
</DataTemplate>
