<DataTemplate xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
              xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
              xmlns:platform="clr-namespace:Microsoft.VisualStudio.PlatformUI;assembly=Microsoft.VisualStudio.Shell.15.0"
              xmlns:loc="clr-namespace:PipelinesExplorer.VisualStudio.Resources;assembly=PipelinesExplorer.VisualStudio">
    <DataTemplate.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource {x:Static platform:EnvironmentColors.ToolWindowTextBrushKey}}" />
            <Setter Property="FontFamily" Value="{DynamicResource VsFont.EnvironmentFontFamily}" />
            <Setter Property="FontSize"   Value="{DynamicResource VsFont.EnvironmentFontSize}" />
            <Setter Property="TextOptions.TextFormattingMode" Value="Display" />
        </Style>

        <DataTemplate x:Key="BranchItemTemplate">
            <TextBlock Text="{Binding Title}" Margin="2,3,2,3" />
        </DataTemplate>
    </DataTemplate.Resources>

    <StackPanel Orientation="Vertical"
                MinWidth="420"
                Margin="12,12,12,8">
        <TextBlock Text="{Binding Prompt}"
                   TextWrapping="Wrap"
                   Margin="0,0,0,10" />

        <ComboBox ItemsSource="{Binding Choices}"
                  SelectedItem="{Binding SelectedChoice, Mode=TwoWay}"
                  ItemTemplate="{StaticResource BranchItemTemplate}"
                  AutomationProperties.Name="{x:Static loc:Strings.A11y_BranchPicker_ComboBox_Name}"
                  MaxDropDownHeight="380"
                  HorizontalAlignment="Stretch"
                  MinHeight="32" />
    </StackPanel>
</DataTemplate>
